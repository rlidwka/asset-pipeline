// Generated by CoffeeScript 1.2.1-pre
(function() {
  var Path, cssimport;

  cssimport = require('../cssimport');

  Path = require('path');

  module.exports = {
    source: 'styl',
    target: 'css',
    compile: function compile(code, options, callback) {
      return cssimport.search_deps(code, options, 'styl', function(err) {
        var stylus;
        if (err) return callback(err);
        try {
          stylus = require('stylus');
          return stylus(code).set('paths', [Path.dirname(options.filename), options.pipeline.builddir]).set({
            filename: options.filename
          }).render(callback);
        } catch (err) {
          return callback(err);
        }
      });
    }
  };

}).call(this);
